#ifndef MATCHES_H
#define MATCHES_H

#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <time.h>

void rules();
int maxMatches(int matches);
int turn(int player, int matches);
void game(int mode);

#endif
